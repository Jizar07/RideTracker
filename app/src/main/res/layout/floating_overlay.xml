<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/floating_overlay_root"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#AA000000"
    android:padding="8dp">

    <!-- Debug preview: shows the captured image with a red border -->
<!--    <ImageView-->
<!--        android:id="@+id/debugImage"-->
<!--        android:layout_width="200dp"-->
<!--        android:layout_height="200dp"-->
<!--        android:scaleType="fitCenter"-->
<!--        android:contentDescription="Debug capture preview" />-->

    <!-- Final calculations overlay (will be updated with final text) -->
    <TextView
        android:id="@+id/overlayText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:singleLine="false"
        android:scaleType="fitCenter"
        android:text="Calculations"
        android:textColor="#FFFFFF"
        android:textSize="14sp" />
</LinearLayout>
